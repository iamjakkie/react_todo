import React from 'react'

export default function TodoInput () {
    return (
        <header>
            <input placeholder="What needs to be done?" />
            <button>Add</button>
        </header>
    )
}